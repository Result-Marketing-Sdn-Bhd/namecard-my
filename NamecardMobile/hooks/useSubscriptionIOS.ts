/**
 * iOS-specific subscription hook
 *
 * Currently just uses the standard useSubscription implementation
 * This file exists for future iOS-specific customizations if needed
 */

import { useSubscription } from './useSubscription';

export const useSubscriptionIOS = useSubscription;
