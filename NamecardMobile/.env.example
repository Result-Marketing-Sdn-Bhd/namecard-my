# Environment Variables Example
# Copy this file to .env, .env.development, .env.staging, or .env.production

# Supabase Configuration
# ⚠️ CRITICAL: Must use EXPO_PUBLIC_ prefix for runtime access in Expo apps
# Variables without this prefix are only available at build time
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Legacy names (kept for backward compatibility with some services)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Vision API (for OCR)
# ⚠️ Use EXPO_PUBLIC_ prefix for runtime access
EXPO_PUBLIC_GEMINI_API_KEY=your_gemini_api_key
GOOGLE_VISION_API_KEY=your_google_vision_api_key
GEMINI_API_KEY=your_gemini_api_key

# OpenAI API (for voice notes and AI features)
OPENAI_API_KEY=your_openai_api_key

# App Environment
APP_ENV=development
DEBUG_MODE=true

# API Endpoints (if using custom backend)
API_BASE_URL=http://localhost:3000

# Sentry (Error Tracking)
SENTRY_DSN=your_sentry_dsn

# Revenue Cat (In-App Purchases)
REVENUECAT_API_KEY=your_revenuecat_api_key

# iOS In-App Purchase Secrets
# ⚠️ CRITICAL: Get this from App Store Connect
# Path: App Store Connect > Your App > App Information > Shared Secret
# Used for server-side receipt verification
IOS_APP_STORE_SHARED_SECRET=your_ios_app_store_shared_secret

# Android In-App Purchase
# ⚠️ NOTE: Google Play no longer uses the old "License Key" approach
# Modern approach requires:
# 1. Google Play Billing Library v5+ in your app
# 2. Server-side verification via Google Play Developer API
# 3. Real-time Developer Notifications (RTDN) for subscription events
#
# Setup required:
# - Enable Google Play Developer API in Google Cloud Console
# - Create service account with appropriate permissions
# - Use OAuth 2.0 for server-side API calls
#
# The old ANDROID_PLAY_LICENSE_KEY is DEPRECATED and no longer needed

# Analytics
ANALYTICS_ENABLED=false
MIXPANEL_TOKEN=your_mixpanel_token